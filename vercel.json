{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd apps/relocation && pnpm run build",
  "devCommand": "cd apps/relocation && pnpm run dev",
  "installCommand": "pnpm install --filter=relocation...",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}
